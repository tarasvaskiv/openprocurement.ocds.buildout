modules:
  input:
    module: wishbone.module.input.couchdbpoller
    arguments:
      couchdb_url: ""
      seqfile: /home/yshalenyk/work/op/ocds-api/openprocurement.ocds.buildout/var/seqfile
  
  fanout:
    module: wishbone.module.flow.fanout

  export:
    module: wishbone.module.process.galleon
    arguments:
      schema: 
      mapping: 
      tagger: ocds

  output:
    module: wishbone.module.output.couchdbpuller
    arguments:
      couchdb_url: ""

routingtable:
  - input.outbox -> export.inbox
  - export.outbox   -> output.inbox  